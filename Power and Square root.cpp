#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a = 2;
    int b = (1,1,1,1);
    cout<<"Power: "<<pow(a,2)<<endl;// This is called Power Formula.......
    cout<<"Square Root of two points is: "<<sqrt(pow(b-b,2)+pow(b-b,2))<<endl;
return 0;
}
