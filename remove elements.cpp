#include <iostream>
using namespace std;

int removeSmallExpenses(double amt)
{
    int count = 0;
    for (int i = 0; i < item.amount.size(); i++) {
        if(amt <= item.amount.get(i))
        {
            item.amount.remove(i);
            item.description.remove(i);
            item.serial.remove(i);
            ++count;
        }
    }
  return count;
}
int main()
{
    cout<<" ";
    cin>>" ";
return 0;
}
